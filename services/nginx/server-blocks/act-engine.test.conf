# upstream clusteractengine {
# 	server act-engine:3333;
# }

# server {
#     listen 80;
#     listen [::]:80;
#     server_name act-engine.test;

#     location / {
#         proxy_pass http://clusteractengine;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Forwarded-Host $host;
#         proxy_set_header X-Forwarded-Port $server_port;
#         proxy_redirect off;
#         proxy_cache_bypass $http_upgrade;

#         # WebSocket support (if needed)
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }

#     error_log /var/log/nginx/act_engine_error.log;
#     access_log /var/log/nginx/act_engine_access.log;

#     client_max_body_size 2048M;
# }

server {
    listen 80;
    server_name act-engine.test;

    root /var/www/act-engine.test/www;  # Adjust to your document root
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    # Optional: Cache control for static assets
    location ~* \.(html|css|js|jpg|jpeg|png|gif|ico|svg)$ {
        expires 30d;
        add_header Cache-Control "public";
    }

    # Optional: Enable Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
